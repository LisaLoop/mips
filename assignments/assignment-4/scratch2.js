const stringOfDigits = (string) => {
        
        return number
}

stringOfDigits('1234')
// read input from user as ascii characters
syscall

// subtract 48 from each character
var m = 1;
for(c = addressOf(str); c != 0; c++){
        str[c]-=48;
        m *= 10;
}
// make each amount multiplied
// by magnitude of each column
var length = c - addressOf(str) //
let acc = 0; 
for(c = addressOf(str); c != 0; c++){
        acc += m * str[c];
        m /= 10;
}
var c1 = 5 * 1000;
var c2 = 6 * 100;
var c3 = 7 * 10;
var c4 = 0 * 1;
// add things together into an accumulator
var number = c1+c2+c3+c4;